export interface Conf {
  dbHost: string;
  dbPort: number;
  dbUser: string;
  dbPassword: string;
  dbName: string;
  serialPort: string;
  serverPort: number;
  dbPoolLimit: number;
  serverUrl: string;
}
